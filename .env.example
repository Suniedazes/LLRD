SITE_INDEXABLE=false

LLRD_LOCAL_STUDIO=false
# npm run studio generates LLRD_STUDIO_TOKEN for the local session.

# Production remains closed until owner approval.
LLRD_AI_ENABLED=false
OPENAI_API_KEY=
LLRD_AI_MODEL=
# Approved inquiry recipient: contactus@llrd.ai (content/company.ts).
# Mailbox/MX delivery is NOT verified. Provider not selected.
# Contact collection is locked in lib/contact-delivery.ts pending privacy + delivery.
# Do not add provider-specific variables until the owner selects a provider.
# Never expose secrets through NEXT_PUBLIC variables.
